[package]
name    = "kestrel-proto"
version = "0.1.0"
edition = "2021"

[dependencies]
serde   = { workspace = true }
bincode = { workspace = true }
anyhow  = { workspace = true }
hmac    = "0.12"
sha2    = "0.10"
